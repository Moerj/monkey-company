// 将element的时间区域组件,输出到2个变量
export default {
    name: 'time-range',

    // 点击后静止按钮,冷却倒计时
    // 用于获取验证按钮
    bind(el, binding, vnode) {
        // console.log(vnode);
        // let vm = vnode.context
        // let vModelKey = vnode.data.model.expression
        
        // vm.$watch(vModelKey,function(v){
        //     console.log('watch:',v);
        // })
    }
}